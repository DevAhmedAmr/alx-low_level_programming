#include"main.h"
/**
 * add - function that add to diffrenct numbers
 * @x: int value
 * @y: type int
 *
 * Return: the the result
 *
 */
int add(int x, int y)
{
	int result = x + y;

	return (result);
}
