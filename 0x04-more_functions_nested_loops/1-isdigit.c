/**
 * _isdigit - function that checks for a digit
 *
 * Return 0 succsess , else failed
 *
 */
int _isdigit(int c)
{
	int i;

	for (i=0; i<=9; i++)
	{
	       	if (i == 9)
		return 1;
	}
	return 1;
}
